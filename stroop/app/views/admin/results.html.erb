<!DOCTYPE html>
  <html>
      <head>
          <title>COLOUR ME</title>
          <link rel="shortcut icon" type="image/png" href="img/icon.png">
          <%= stylesheet_link_tag "bootstrap.min" %>
          <%= javascript_include_tag "bootstrap.min" %>
      </head>
      <body onload="randomizeColour();setColourTable();">
	      <div class = "main">
	      	<table class = "table table-striped float" style="width:500px; margin-top:50px;" >
				<tr>
					<td><b>#</b></td>
					<td><b>match</b></td>
					<td><b>mismatch</b></td>
				</tr>
				<% @results.each do |r| %>
					<tr>
					<td> <%= r.id %> </td>
					<td> <%= r.match %> </td>
					<td> <%= r.mismatch %> </td>
					</tr>
				<% end %>
			</table>
		  </div>
      </body>
  </html>
